export const name = 'dsh-gemini-m3e-theme-service'

// This bundle is browser-only: the Client half in src/client.js owns the theme,
// token overrides, and the floating toggle. The Host half registers nothing.
export function apply() {}